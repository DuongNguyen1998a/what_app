import 'package:flutter/material.dart';

class CallsContainer extends StatelessWidget {
  const CallsContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
